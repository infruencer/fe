/**
 * 모달창 타입
 */
export enum ModalType {
  MODAL = 'MODAL',
  ALERT = 'ALERT',
  CONFIRM = 'CONFIRM',
}

/**
 * 모달 메시지 타입
 */
export enum MessageType {
  DEFAULT = 'DEFAULT', // 기본
  CONFIRM = 'CONFIRM', // 컨펌
  ERROR = 'ERROR', // 에러
}
